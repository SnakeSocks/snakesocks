[core]
# The port our server monitor on.
port = 8080
# The encode and decode module.
module = /etc/snakesocks/modules/simple_unsafe_encrypt.so
passphrase = p@ssw0rd
[extra]
# Debug Level: 1(Only Panic message),2(Error),3(Connect Logs)
debugLevel = 2
